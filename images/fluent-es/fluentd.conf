<source>
  @type forward
</source>

<match nginx.docker.**>
  type parser
  format nginx
  remove_prefix nginx
  key_name log
  reserve_data yes
</match>

<match docker.**>
  type elasticsearch
  hosts http://elasticsearch:9200
  logstash_format true
</match>
